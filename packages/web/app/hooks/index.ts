// Utility hooks (UI-focused, not merged into stores)
export { useIsMobile } from './use-mobile';
export { useStickyHeaders } from './use-sticky-headers';

// Note: useNotes and useSse have been replaced by stores:
// - useNotes -> useDevlogNotes from @/stores
// - useSse -> useRealtime from @/stores
