{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**", ".next/**"],
      "env": [
        "DATABASE_URL",
        "NODE_ENV",
        "NEXT_BUILD_MODE",
        "npm_package_version"
      ]
    }
  }
}